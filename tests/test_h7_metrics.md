# `test_h7_metrics.py`

This test constructs a passing synthetic report and then removes the declared
60% position improvement. It ensures absolute accuracy alone cannot pass H7
when the new bounded mechanism is not causally better than its frozen backbone.
